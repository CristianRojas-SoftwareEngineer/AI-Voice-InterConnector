[package]
name = "xtask"
version = "0.1.0"
edition = "2021"
publish = false
description = "Tareas de desarrollo (generación de Cask, SOURCE-OFFER, licencias)"

[dependencies]
clap = { version = "4.4", features = ["derive"] }
anyhow = "1.0"
regex = "1.10"
